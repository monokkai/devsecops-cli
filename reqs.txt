cargo build --release - building
sudo cp target/release/devsecops-cli /usr/local/bin/ - coping binary file
monokkai --help - checking

Rebuild:

```zsh
cargo clean
cargo build --release
sudo cp target/release/monokkai /usr/local/bin/
```